# 栽培予測システム — 合意仕様（正本）

最終更新: 2026-09-12（§0.8 約束≤実力 · §0.9 チャート視覚）

このファイルが **製品判断の正本**。チャット要約・キャンバス・コードコメントより優先する。  
矛盾する実装を入れるときは、先にこのファイルを更新し、ユーザーの確認を取る。黙って上書きしない。

対象画面: 予測 `inventory.php` / 需給 `capacity.php` / 今日 `today.php` / 精度 `agent.php` /
実収穫量 `actual.php` / 生育日 `growth_days.php` / 経営アラート `alerts.php`

---

## 0. 高次機能とクリティカルパス（FAST）

### 0.1 上位ニーズ

| ニーズ | 意味 |
| --- | --- |
| **取引先が安心する** | 約束どおり届く。不足や急な欠品に振り回されない |
| **従業員が安心する** | 何をすればよいか分かり、自ら率先して動ける（現場はベトナム人スタッフが中心） |
| **経営・営業が遠隔で把握できる** | 経営者・営業担当は常に圃場にいない。システムの稼働で**圃場の状態・過不足・先行きが可視化**され、現地にいなくても判断できる |

三つの安心（取引先・従業員・経営）が揃って、高次機能が成立する。可視化は「おまけのダッシュボード」ではなく、経営ニーズそのもの。

### 0.2 高次機能

**率先できる現場と、安心できる供給を確立する。**

言い切り: 従業員が安心してやるべきことを自ら率先してやれる環境を実現し、結果として取引先に安心して供給できる栽培管理手法を確立する。

機能樹の根（左端）はこの高次。そこから **3ニーズに分岐**し、各枝が同じ階層で画面まで落ちる。

- **ベトナム語で使えること**は装飾ではなく、従業員が安心して率先するための**前面イネーブラー**（枝Bの前提）。出荷・在庫の本線と同列に設計する。
- **状況の可視化**（予測・需給・今日・栽培）は、経営・営業が現地にいなくても判断するための**前面機能**。現場向け操作と経営向け見通しを同じデータでつなぐ。

### 0.3 機能樹の階層（レベル揃え）

| レベル | 問い | 置くもの |
| --- | --- | --- |
| **L0** | 何を確立するか | 高次機能（唯一・樹の根） |
| **L1** | 誰の安心か | 3ニーズ（取引先／従業員／経営） |
| **L2** | 何のため | 各ニーズの成果状態（**L1 に対し 1:N**。1:1 は分解不足のサイン） |
| **L3** | どうやって | 機能（動詞＋名詞）。本線は枝をまたいで共有 |
| **L4** | 何で実現する | 画面・モジュール（機能マップ） |

**L2 の作り方**: (1) いまの機能・画面に「何のため？」と聞いて上げる (2) L1 に「足りる条件は？」と聞いて下ろす。見つかった不足はギャップ＝今後の機能として樹に載せる。

### 0.3.1 L2 一覧（合意）

**枝A 取引先が安心する**（取引先への画面開示はしない。社内判断用）

| L2 | 主な L3 | L4 例 |
| --- | --- | --- |
| 約束量を畑で裏打ちできる | 出荷コミット確定・在庫過不足を防ぐ | 需給 / 予測 |
| 先行きの変化を早く共有できる | 営業判断を伝える | 需給アラート・③SIM |
| 不足リスクを早く潰せる | 床を空けて回す・能力を計画する | 今日 / 計画 |

**枝B 従業員が安心する**

| L2 | 主な L3 | L4 例 |
| --- | --- | --- |
| 今日のやるべきことが分かる | 今日の作業を進める | 今日 / 栽培 |
| 迷わず実行できる | 判断基準を単純にする・母国語で操作できる | 表示ルール / ベトナム語UI |
| 作業を正しく残せる | 栽培実績を記録する | 収穫・定植入力 |

※「従業員へのフィードバック＝人事評価」ではない。評価制度は本システムの範囲外。登録できたことの確認は「作業を正しく残せる」に含める。

**枝C 経営・営業が遠隔で把握できる**

| L2 | 主な L3 | L4 例 |
| --- | --- | --- |
| いまの圃場状態が分かる | 畑の状態を把握する・今日の作業を見る | 栽培 / 今日 |
| 先の過不足が見通せる | 将来余剰を見通す・能力を計画する | 予測 / 需給 / 計画 |
| 現場が回っていると確認できる | 現場記録が経営画面になる | 同一データ |
| 営業の材料が手元にある | 営業判断を伝える | 需給 |
| **実績パフォーマンスを常時把握できる** | 月次収量・ベッドあたり・**ゴミ量**を見る | **実収穫量**（常時到達可） |
| **異常・要注意にすぐ気づける** | 遅れ・破棄・在庫割れ・データ鮮度を一覧する | **経営アラート** `alerts.php` |

### 0.4 クリティカルパス（本線・HOW）

出荷を満たす → 在庫過不足を防ぐ → 将来余剰を見通す → 畑の能力を計画する → 今日の作業を進める → 栽培実績を記録する

左ほど「なぜ」、右ほど「どうやって」。新機能は「本線のどの箱の HOW か」／「どの L2 の HOW か」を答えてから入れる。樹の L3 では「本線」として複数枝に現れるが、実体は1本。

### 0.5 情報設計（層分け）

| 層 | 置くもの | 例 |
| --- | --- | --- |
| **前面（毎日）** | 本線の実行・見通し | 今日 / 栽培 / 予測 / 需給（＋計画・入力） |
| **経営常時** | 遠隔把握のうち「いつも開ける」もの | **実収穫量**（下部ナビ「収量」）／**経営アラート**（メニュー先頭・ホーム） |
| **イネーブラー** | 率先の前提 | **ベトナム語UI** |
| **診断（週次〜）** | 改善・深掘り | 生育日／精度の詳細。実収穫・ゴミは診断にも使うが**置き場所は経営常時** |
| **基盤** | 予測の前提・マスタ | 気温同期 / ベッド稼働 |
| **周辺** | 監査・将来拡張 | 精度の深い監査 / 生育写真 |

- **実収穫量・ゴミ量**: 予測ロジックの入力でもあるが、経営判断用に**常に見られる**位置にする。取引先への開示はしない。
- **経営アラート** `alerts.php`: 定植猶予超過・破棄候補・累計余剰の割れ接近・気温鮮度を1画面に集約（`lib/mgmt_alerts.php`）。判定は既存ロジックの束ね。取引先非開示。
- 前面ナビに診断・基盤・周辺を同列で並べない。**経営常時の実収穫量は下部ナビ「収量」で1タップ**（メニュー先頭の「経営常時」からも可）。

### 0.6 次に拾うギャップ（未実装・設計候補）

| 項目 | 枝 | メモ |
| --- | --- | --- |
| ~~経営アラート一覧~~ | C | **対応済**: `alerts.php`（遅れ・破棄・割れ・気温） |
| ~~実収穫量の到達改善~~ | C | **対応済**: 下部ナビ「収量」＋メニュー「経営常時」 |
| 入力直後の載った確認 | B | 人事評価ではない。登録完了の短いフィードバック |
| ~~約束≤実力の一言サマリ~~ | A/C | **対応済**: `lib/promise_capacity.php`（予測・需給・アラート） |

### 0.7 画面内UI原則（ページ中身）

メニューの層分け（§0.5）に加え、**各ページ内**も FAST の L2 に揃える。

| 原則 | 内容 |
| --- | --- |
| **1画面1主目的** | タイトル直下に主L2（何のため）を1行。複数の「何のため」を同列に並べない |
| **Above the fold** | 最初の画面に載せるのは、そのL2の判断または次アクションだけ。表・深いチャートは二次 |
| **層を混ぜない** | 前面ページに診断・基盤・周辺の操作を埋め込まない。リンクで逃がす |
| **枝で言葉を変える** | 現場＝作業・空き日数。営業＝一時／トレンド／遅れ。経営＝実力・異常 |
| **取引先非開示** | 収量・ゴミ・経営アラートは社内。取引先共有用の数字は「共有用」と明示 |

**画面ごとの主L2（中身の北極星）**

| 画面 | 主L2 | Above the fold |
| --- | --- | --- |
| 今日 | 今日のやるべきことが分かる | 定植・収穫・破棄の件数とリスト |
| 栽培 | いまの圃場状態が分かる | ベッド一覧（入力は今日へ） |
| 予測 | 先の過不足が見通せる | 約束≤実力＋割れまで＋緑／灰の本線チャート |
| 需給 | 営業の材料が手元にある | 約束≤実力＋営業アラート → 能力チャート |
| 収量 | 実績パフォーマンスを常時把握 | ゴミ込みの実力（年次・月次） |
| 経営アラート | 異常・要注意にすぐ気づける | 約束≤実力＋重大→注意の一覧 |

診断・基盤・周辺画面は「深掘り」専用。前面のコピーや主CTAを持ち込まない。

### 0.8 約束 ≤ 実力（社内サマリ）

- **約束** = GCAL確定出荷（週次）。**実力** = 計画能力（緑＝収穫後5日で次定植した場合）。
- 今後8週の合計と週次超過を見て一言にする（`gf_promise_vs_capacity_summary`）。定植済能力は参考。
- 表示: 予測・需給・経営アラートの Above the fold。**取引先向け画面は作らない**。
- 超過があれば経営アラート一覧にも載せる。

### 0.9 チャート視覚（本線強調）

| 系列 | 色・線 | 役割 |
| --- | --- | --- |
| 計画（緑） | 実線・太 | 本線・公式の先行き |
| 定植済（灰） | 破線・細 | いまの畑／定植催促（公式ではない） |
| GCAL（紫） | 破線 | 約束 |
| 営業現実・SIM（青） | 実線または破線 | 定植済累計・シミュレーション |
| 昨対 | ごく薄い灰 | 参考。主役にしない |

- 共通テーマ: `js/gf-chart-theme.js`。0kg に水平ガイド。主チャートは `chart-card.primary`。

---

## 1. 本線は何か

- **公式の先行き** = 計画どおりに定植した場合の累計在庫（緑）＝ **実績（いまの畑）＋計画（次定植）**。
- **いまの畑だけ**（定植済・灰）が先でゼロに見えるのは、定植催促。公式予測ではない。
- **下チャートの青点線（累計余剰・定植済ベース）は営業判断の現実線**。緑に寄せていく手段は定植計画。青は「今売れる／足りない」の判断に使う。
- 確定出荷は **GCALのみ**。manual は使わない。

## 2. 定植済と計画は、いつ一致するか（必須）

**いま植えても収穫が届かない期間は、定植済累計 ＝ 計画累計。**

- 起点・残出荷・週の切り方を同じにする。系列を別にゼロ起算しない。
- 計画累計 = 予測の累計余剰（既存株・残出荷）を土台に、まだ植えていない将来収穫だけ足す。
- 定植済累計も **同じ土台**。差が出てよいのは、次定植の収穫が実際に載る週から。
- 2026-08時点の目安は **9/20週まで差なし**（栽培日数の結果。日付を固定仕様にしない）。

やってはいけない例:

- 需給②の定植済累計だけ当週ゼロ起算にして、計画より下に見せる。
- 栽培中のベッドの「次定植」収穫を、今サイクルより前の週に載せる。

## 3. 回転と定植遅れ

- 収穫後 **5日前後** に次定植する計画を、画面ボタンなしで自動維持する（`GF_REPLANT_GRACE_DAYS = 5`）。能力・累計の計画線はこの前提。
- **定植遅れ**に出してよいのは、空いていて予定日を過ぎたベッドだけ。
- **栽培中のベッドは定植遅れに出さない。** 植えられない。出すのは破棄リスクのみ。

### 3.2 定植予定の超過と緑（将来予測）の分離（必須）

**問題**: `plant_schedule.planned_plant_date` がすでに過去なのに、その日付から収穫週を計算すると、**収穫できない週に計画kgが載る**（例: 8/29定植予定のまま 10/25 収穫を緑に残す）。

**分離**:

| 用途 | 超過した定植予定の扱い |
| --- | --- |
| **定植待ちパネル／今日の作業** | **空きになってからの日数**を主表示する。運営目標は **空き後 `GF_REPLANT_GRACE_DAYS`（5日）以内に定植**。予定日と緑起点の二重表示で迷わせない |
| **緑（能力・累計の将来予測）** | DBの過去予定日は使わない。**現実に植える起点**から収穫週を載せる |

**緑の現実起点（v1）**:

- 予定日 `< 今日` の planned/approved 行は、緑計算だけ **有効定植日 = 明日**（`today + 1 day`）に置き換えて収穫週・kgを載せる。
- 理由: 当日作業の残り時間を見ると「今日植えた」より「明日植えた」の方が過大評価しにくい。DBの `planned_plant_date` 自体は書き換えない。
- 予定日が今日以降なら、そのままの予定日で緑に載せる。
- 仮想連続定植（rotation）の「予約後の次サイクル」起点も、同じ有効定植日＋栽培日数に合わせる（過去日のまま早すぎる空きを作らない）。

**定植待ちパネル（v1）**:

- 主指標は **空き日数**（直前サイクルの `harvest_end` → 今日）。5日超で遅れ表示。
- `planned_plant_date` はDB・内部スケジュール用。パネルの主ラベルに「予定○日・緑は明日…」と並べない。

やってはいけない例:

- 緑・能力に、超過した過去の定植予定から計算した収穫を残す。
- 遅れ可視化のために、パネルから超過予定を消す／勝手に予定日を今日へ書き換える。
- パネルで予定日と有効定植日を並べて二重管理させる。

### 3.1 破棄候補の生育写真（v1: 登録・閲覧）

- **対象**: 破棄候補サイクル（`capacity_discard_candidates()` に含まれる未完了 `cycle_id`）に、生育状況写真を**複数枚・撮影日付き**で登録できる。
- **閲覧**: サイクル完了後も写真は残し、記録として閲覧できる。
- **予測・需給・破棄候補ルールには影響しない**（記録のみ。自動破棄しない）。
- 写真は `cycle_id` に紐づける（ベッド名ではなく当該栽培サイクル単位）。

将来（v2・未実装）:

- 写真登録時、過去に最終 outcome が破棄（GOMI）だった事例との**画像類似度**から破棄確率を推定する。
- 推定が閾値以上なら**破棄実行を提示**する（自動実行はしない。人が需給画面で確定）。
- 教師データの outcome は `harvests` の GOMI／通常収穫から自動導出する。

## 4. 営業（余りと減）

- 営業が重視するのは **青点線（定植済累計余剰）の直近**。緑は定植計画の到達点。
- 当週〜直後の定植済プラスは、先の出荷の持ち越し。スポットで売らない。
- 先々の余りは **計画ライン（緑）** で拡大提案する（実行が追いついているとき）。
- **秋〜冬など同世代が弱い局面で、青点線だけが楽観のまま残るのは営業リスク**（§6の同世代補正の動機）。
- 低温期の能力減は、在庫割れを待たず早めに営業へ。開始週が10〜2月のときだけ「低温」と書く。8月を低温としない。
- 営業アラートのラベルは **一時** / **トレンド** / **遅れ** のみ。
- トレンドは連続した週を1行にする（開始週〜終了週、kg/週）。一時は週ごと。
- 減少トレンドが累計余剰で賄えるときは、その旨と「ベッドあたり収穫量が ●●kg を下回ると累計がマイナスになる」を付ける。賄えないときはその旨を書く。
- 拡大より減少の伝達を優先する。

## 5. 需給シミュレーション③

意図（実装が違っていても、仕様はこちら）:

| ボタン | 意味 |
| --- | --- |
| ① 一時を直載せ | アラートのうち **一時** を、該当週のGCALにそのまま足し引きする。トレンドは載せない |
| ② 平準化（弱） | **一時ではない** 数か月トレンドを均す。③と同じ計算で、週あたり幅は半分 |
| ③ 平準化（強） | ②と同じ均しを満額で載せる |

直載せにトレンドだけ載せて一時を除外するのは、仕様違反。

## 6. 予測kg・日数の起点

- 予測の日数・収量の起点は **定植日**（`plant_date`）。収穫予定 = 定植 + pred_days。
- **播種日（`sow_date`）は入力・履歴として残す。現状の予測モデル特徴量には使わない**（育苗待ち日数を特徴に載せない。固定値プレースホルダのみ可）。
- 当週・翌週: 栽培中再予測のkg。
- 2週先以降: 日数は再予測、kgは定植時モデル。部分収穫の⑤は上書きしない。
- 完了サイクルへの後付け予測でMAPEを作らない。**完了サイクルの実績kgは変更しない。**

### 6.1 同世代補正（未収穫のみ・営業リスク低減）

目的: 同時期定植の実績が定植時予測を下回り続けるとき、**未収穫の同世代**の予定kgが楽観のまま青点線に残るのを防ぐ（特に秋〜冬）。

- **対象**: 未収穫サイクルのみ。完了床の実績は触らない。
- **単発では動かさない**: 同世代で複数床の実績が揃ってから。1床だけでは補正しない。
- **部分収穫（⑤・途中）**: 同世代補正の**証拠にも発火にも使わない**（他ベッドへ伝播しない）。そのベッド単体の最終予測への反映は従来どおり可。上振れの引き上げは必須ではない。
- **部分の最後（全部・完了）**: 面積が全体になりサイクルが閉じた時点で、その床は**完了実績**として証拠に数える。乖離条件を満たせば同世代の未収穫へ発火してよい（完了床の実績kg自体は書き換えない）。
- **緑への反映**: 緑＝実績＋計画。同世代補正は未収穫の実績側（定植済）に載せる。再予測するなら計画側も同じ前提で変わる（青だけ下げて緑を据え置かない）。
- **記録**: 補正した場合は理由（同世代・床数・乖離）をログ／payloadに残す。画面バッジは必須にしない（「明示」＝黙って上書きしないための記録）。

発火閾値（v1）:

| 項目 | 値 |
| --- | --- |
| 同世代窓 | 定植日 **±5日** |
| 優先範囲 | 同一 `group_type` 優先。ピア不足時のみ全体 |
| 証拠床 | **完了床のみ**（途中の部分⑤は数えない。全部で閉じた床のみ） |
| 最低証拠数 | 同世代で乖離条件を満たす完了床 **≥2** |
| 乖離条件 | 実績合計kg／定植時予測kg **< 0.90** |
| 補正先 | 未収穫の同世代のみ。完了実績は触らない。上振れ伝播なし |
| 補正の強さ | 証拠床の中央値比率へ寄せる。フロア **max(0.75, median_ratio)** |
| 季節ゲート | なし（通年同一ルール） |

## 7. 実収穫量・生育日の年次モニター

- **実収穫量** `actual.php`: 収穫月ごとの kg・ベッド数の年次比較。
- **経営常時の可視化対象**（§0.5）。予測の入力でもあるが、経営判断のため**いつでも開ける**。取引先には開示しない。
- **ベッドあたり・月合計のパフォーマンスは GOMI（ゴミ）を含む。** 出荷可能在庫とは別。ゴミkgは内訳として常時見える（診断用途＋経営常時。取引先非開示）。
- **生育日** `growth_days.php`: **定植→収穫開始** 日数の年次比較。画面上部で月の归属を切替:
  - **収穫日ベース** … 収穫開始月で集計（実績トレンド監視）
  - **定植日ベース** … 定植月で集計（Excel時代の「同時期定植の見え方」可視化）
- 主指標は定植→収穫開始。定植→収穫完了は参考。播種起点の日数は主モニターにしない。
- 定植日ベースは **可視化のみ**。予測への反映は特徴量（前年同時期日数・類似ベッド平均日数）側。モニター切替は手作業予測の代替ではない。
- 月別の **ばらつきSD** は、月内の生育日数の散らばり。目安は回転1スロットの **5日前後**、**10日未満**なら平常幅、**10日以上**は切り分け優先。同月の平均・前年比とセット。画面注釈に記載。

## 8. 気温データ（UltraNet / weather_daily）

- 予測特徴量の気温は **`weather_daily` の実測**（将来予報は使わない）。
- 取得元: **UltraNet（UTNET）** `https://sedia-utnet.com` / ハウス「水耕ハウス」(id=6229)。
  - 日次CSV（センサデータ・過去3ヶ月・平均24時間）の **温度1〜4地点平均** を `temp_avg`、各地点最大の最大を `temp_max`、各地点最小の最小を `temp_min` とする。
  - JOB: `jobs/sync_ultranet_weather.php` → 続けて鮮度チェック。認証は `db.local.php` の `ultranet_user` / `ultranet_pass`（秘密は正本に書かない）。
  - **自動取得**: 本番 cron **毎日 08:10** sync → **08:20** 鮮度チェック（sync 成功時も直後に check を実行）。ログ: `/home/love-media/forc_logs/weather_cron.log`
- **取得失敗・更新停止は即時アラート**する。黙って止まったままにしない。
  - 判定: 当日バッチ時点で `MAX(date) < 昨日`、または取得JOB自体の失敗。
  - 記録: `alerts(type=data_missing, kind=weather_stale|weather_fetch_fail)` ＋運用ログ。設定があればメール通知（宛先: `db.local.php` の `alert_to`）。
  - 予測・気温比較画面にも滞留バナーを出す。
- **優先順位**: UltraNet から取れる期間は **実測取込を優先**。類推埋めしない。
- **類推埋めは、UltraNet から取れない過去欠損だけ**（予測を止めない応急）。条件:
  - UltraNet上も0／欠測の日を含む。取れる実測がある日は類推しない。
  - 前後の実測がある短い欠損は直線補間を優先。
  - 埋めた行は **`is_imputed=1` 必須**。実測が来たら上書き置換。
  - 補完してもアラートは出す（補完は黙殺の代替ではない）。
  - 気温比較の「実測トレンド」表示では補完を明示する。
## 9. 用語・表示

- 週ラベルは日曜始まり **「8/9週」**（年なし）。日付は `Y-m-d`。
- 画面のkgは整数。日数は整数。
- 定植済予測 / 未収穫ベッド / 残出荷 / 累計余剰。旧称「オープン予測」をUIに出さない。
- 面積比の表示は UI分数（`1/4`〜`3/4`・`全体`）のみ。`%` や `—` は出さない。
  完了サイクルの旧小数は kg 比重で分数へ**DB書き換え**し、合計がちょうど全体になるように揃える（予測には使わない。集計ずれ防止）。

## 10. 変更手順（エージェント）

1. このファイルを読む（特に §0）。
2. 触る画面が 予測・需給・精度・回転・実収穫・生育日・気温・UI言語 なら、§0〜8 に反しないか確認する。
3. 新しい合意が出たら、コードより先にこのファイルを更新する。
4. 会話の「今回の本意」で正本を置き換えない。正本と違うなら確認する。
5. 筋が悪い要望（系列定義の違うチャートを無理に同じ軸に合わせる等）は実装前に指摘する。
6. 機能追加前に §0.4 を問う: 「クリティカルパス／どの L2 の HOW か」。診断・基盤・周辺なら前面ナビに出さない。L2 が L1 に対し1つしか無いときは分解を疑う（§0.3）。
